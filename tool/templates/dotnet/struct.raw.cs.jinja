// <auto-generated/> by Diplomat
using System;
using System.Runtime.InteropServices;
using {{ namespace }};
using {{ namespace }}.Diplomat;

namespace {{ namespace }}.Raw;

[StructLayout(LayoutKind.Sequential)]
internal partial struct {{ name }}
{
    {%- for field in fields %}
    {#- `raw_field_type()` maps `bool` to the blittable one-byte `DiplomatBool`
        so this by-value struct stays blittable across a P/Invoke on .NET
        Framework (a `bool` field would make it non-blittable). Rust's
        `repr(C)` bool is one byte, which `DiplomatBool` matches exactly. #}
    public {{ field.raw_field_type() }} {{ field.name }};
    {%- endfor %}


    {%- for method in methods %}

    {% include "dotnet/raw_extern.cs.jinja" %}
    {%- endfor %}
}
